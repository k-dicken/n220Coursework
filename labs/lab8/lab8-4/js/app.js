// Create a function that takes a word, and turns it into the pig latin version. (To do so: 1) identify the first vowel in a string, 2) remove all the characters from before that vowel and add them to the end of the word.  3) Add "ay" to the end of the string. )
// Write a web page to test the function - one button and input, and one div. When the button is pressed, take the input, use the function to remove the hashtags, and show the result string on the page.

//

//grab input
//pigLatinString = ""

//loop
//if letter doesnt equal vowel
    //

//

let input = "string";
let pigLatinString = "";

input[0] = "r";

console.log(input[0]);

// function pigLatin() {

    // for(i = 0; i < input.length; i++) {
    //     console.log(i);
        
    //     if (input[i] ==  "i" ) {
    //         pigLatinString = pigLatinString + input;
    //         console.log(input[i]);

    //     } else {
    //         pigLatinString = pigLatinString + input[i];
    //         input[i] = "";
    //     }

    //     console.log(input);
    // }

   
// }

// pigLatin(input);

// console.log(input);
// console.log(pigLatinString);

